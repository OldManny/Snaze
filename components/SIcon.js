import React from 'react';

// Render a styled "S" icon
const SIcon = ({ className }) => (
  <div className={`s-icon ${className}`}>
    S {/* Display the letter "S" */}
  </div>
);

export default SIcon;
